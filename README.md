# goGryta

> To stir the pot

```
./gogryta -h
  -case string
        lower or upper (default "mix")
  -exact
        for exact character count
  -len int
        lengt of returned password (default 15)

```